# .env.example
# Скопировать в .env и заполнить.
# .env загружается автоматически при `uv run`.

# Обязательно: пароль PostgreSQL
PG_PASSWORD=

# Опционально: пароль внешнего NTRIP-кастера (если upstream.enabled=true)
NTRIP_UPSTREAM_PASSWORD=
