"""Протокольные адаптеры над внешними парсерами.

* NMEA — используем ``pynmeagps`` (XOR-checksum валидируется внутри);
* RTCM3 — используем ``pyrtcm`` (CRC24Q-валидация и reassembly внутри).

Задача этого слоя — принять объект от внешнего парсера и превратить его
во фрагменты доменной модели (GGA → позиция + solution_mode + age,
GST → σE/σN/σU, GSA/GSV → спутники и DOP).
"""
